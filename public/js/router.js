// Generated by CoffeeScript 1.7.1
(function() {
  var __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  Tracker.Routers.Main = (function(_super) {
    __extends(Main, _super);

    function Main() {
      return Main.__super__.constructor.apply(this, arguments);
    }

    Main.prototype.routes = {
      "home": "home",
      "tables": "tables",
      "play": "play",
      "*page": "home"
    };

    Main.prototype.main_el = "#main";

    Main.prototype.loader_el = "#loader";

    Main.prototype.initialize = function() {
      this.app = new Tracker.Views.App();
      this.app_view = this.app.render();
      this.toggle_loader();
      return this;
    };

    Main.prototype.home = function() {
      this.home = this.app.views['home'] || new Tracker.Views.Home();
      return this.app.push(this.home);
    };

    Main.prototype.tables = function() {
      this.tables = this.app.views['tables'] || new Tracker.Views.Tables();
      return this.app.push(this.tables);
    };

    Main.prototype.play = function() {
      if (!Tracker.Instance.user.hasOwnProperty('playing')) {
        Tracker.Instance.main_router.navigate('', {
          trigger: true
        });
        return false;
      }
      this.play = this.app.views['play'] || new Tracker.Views.Play();
      return this.app.push(this.play);
    };

    Main.prototype.toggle_loader = function() {
      return $(this.loader_el).fadeOut();
    };

    return Main;

  })(Backbone.Router);

}).call(this);
